export interface SvgPathProps {
    icon: string;
    color: string;
    size: number;
}
[]
export interface HomeInterface {
    logo: string;
    title: string;
    background? : string;
    message: string;
}